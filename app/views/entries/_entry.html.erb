<%= turbo_frame_tag dom_id(entry) do %>
  <li><%= entry.text %></li>
  <%= turbo_frame_tag dom_id(entry, :actions) do %>
    <%= link_to "Delete", [journal, entry], method: :delete, data: { confirm: 'Are you sure?' } %>
  <% end %>
</%>
